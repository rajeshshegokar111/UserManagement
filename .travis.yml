language: nodejs
node_js:
  - 0.12.7
cache: npm

install: 
  - npm install

script:
  - npm test

deploy: 
  provider: pages
  skin-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
